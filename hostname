arch-chroot /mnt /bin/bash << EOF
  # Set machine name
  echo $HOSTNAME > /etc/hostname

  # Setup hosts file
  cat <<- HOSTS >> /etc/hosts

127.0.0.1	localhost
::1		localhost
127.0.1.1	${HOSTNAME}.localdomain ${HOSTNAME}
  HOSTS
EOF
